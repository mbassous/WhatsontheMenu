<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mbccjlkn.whatsonthemenu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DiningHallSelection" />
        <activity android:name=".CafeSelection" />
        <activity android:name=".FoodTruckSelection" />
        <activity android:name=".Map" />
        <activity
            android:name=".DHDisplay"
            android:label="@string/title_activity_dhdisplay"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CafeDisplay"
            android:label="@string/title_activity_cafe_display"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FoodTruckDisplay"
            android:label="@string/title_activity_food_truck_display"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>